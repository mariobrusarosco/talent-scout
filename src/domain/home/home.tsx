import { Button } from "../study-and-validation/Button/Button";

const HomeScreen = () => {
  return (
    <section>
      <h1>Talent Scout</h1>

      <Button />
    </section>
  );
};

export default HomeScreen;
